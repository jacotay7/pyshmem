[pytest]
testpaths = tests
addopts = -ra --strict-markers
markers =
    cpu: tests that exercise CPU-backed shared memory behavior
    benchmark: performance-oriented benchmark tests
    gpu: tests that require a CUDA-capable PyTorch installation
